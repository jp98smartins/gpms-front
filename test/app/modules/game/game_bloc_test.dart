void main() {
  // blocTest<GameBloc, int>('emits [1] when increment is added',
  //   build: () => GameBloc(),
  //   act: (bloc) => bloc.add(GameEvent.increment),
  //   expect: () => [1],
  // );
}
