void main() {
  // blocTest<MenuBloc, int>('emits [1] when increment is added',
  //   build: () => MenuBloc(),
  //   act: (bloc) => bloc.add(MenuEvent.increment),
  //   expect: () => [1],
  // );
}
